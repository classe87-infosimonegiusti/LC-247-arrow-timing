//esempio con arrow function su una riga
//setTimeout(() => alert('Sono passati 3 secondi!'), 3000);

setTimeout(funzioneDaRichiamare, 3000);

function funzioneDaRichiamare() {
    alert('Sono passati 3 secondi!');
}